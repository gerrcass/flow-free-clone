# Web-first instead of native iOS

Started as a native iOS clone, pivoted to a web app for v1 per user decision to validate the core Pipe mechanic faster with zero install and deploy friction.

## Considered Options

- Native Swift + SwiftUI for iOS 17+ (original direction)
- Web app (chosen): instant playtest loop, no App Store review, portable to iOS wrapper later
